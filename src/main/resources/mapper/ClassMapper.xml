<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.onlineaddressbooksystem.mapper.ClassMapper">
    <select id="query" resultType="com.onlineaddressbooksystem.vo.ClassVo">
        select id, name from class where major_id = #{majorId}
    </select>
</mapper>